//
// Scene.hpp
//
// @author Roberto Cano
//

#pragma once

#include "Scene.hpp"

using namespace Framework::Core;

Scene::Scene(const Size2D& sceneSize)
{
    //setSize(sceneSize);
    //setAnchorPoint(Point2D(0.5f, 0.5f));
    //setWorldPosition(getSize() / 2.0f);
}